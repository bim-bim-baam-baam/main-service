spring:
  application:
    name: main-service
  datasource:
    url: jdbc:postgresql://localhost:5432/errorhack
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop

rest:
  client:
    parsing:
      base-url: http://error-parser:8001

springdoc:
  swagger-ui:
    path: /docs
