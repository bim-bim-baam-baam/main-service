spring:
  application:
    name: main-service
  datasource:
    url: jdbc:postgres://localhost:5432/errorhack
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop

springdoc:
  swagger-ui:
    path: /docs
